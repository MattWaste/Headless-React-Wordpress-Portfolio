import {React} from 'react';
import {initTE,Datepicker} from 'tw-elements-react';

export default function Carousel(){
  useEffect(() => {
    initTE({ Datepicker, Input });
  }, []);
    return (
      <div></div>
    )}